<Window x:Class="MOMO_QR_DANANG.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:MOMO_QR_DANANG.ViewModels"
        mc:Ignorable="d"
        Title="Tạo Mã QR Thanh toán VietQR/MoMo" Height="600" Width="800">

    <Window.DataContext>
        <vm:MainViewModel/>
    </Window.DataContext>

    <Grid Margin="15">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>

        <StackPanel Grid.Column="0" Margin="0,0,30,0">
            <TextBlock Text="Thông tin Thanh toán" FontWeight="Bold" FontSize="20" Margin="0,0,0,15"/>

            <TextBlock Text="Số Tài Khoản:" Margin="0,5"/>
            <TextBox Text="{Binding SoTaiKhoan, UpdateSourceTrigger=PropertyChanged}" Padding="8" FontSize="14" />

            <TextBlock Text="Tên Tài Khoản:" Margin="10,10,0,5"/>
            <TextBox Text="{Binding TenTaiKhoan, UpdateSourceTrigger=PropertyChanged}" Padding="8" FontSize="14" />

            <TextBlock Text="Số Tiền (VNĐ):" Margin="10,10,0,5"/>
            <TextBox Text="{Binding SoTien, UpdateSourceTrigger=PropertyChanged}" Padding="8" FontSize="14" PreviewTextInput="NumberValidationTextBox" />

            <TextBlock Text="Nội Dung Chuyển Khoản:" Margin="10,10,0,5"/>
            <TextBox Text="{Binding NoiDung, UpdateSourceTrigger=PropertyChanged}" Padding="8" FontSize="14" />

            <StackPanel Orientation="Horizontal" Margin="0,20,0,15">
                <Button Content="Chọn Logo" Command="{Binding SelectLogoCommand}" Padding="10" Width="150" FontSize="14"/>
                <Border BorderBrush="LightGray" BorderThickness="1" Width="60" Height="60" Margin="20,0,0,0">
                    <Image Source="{Binding LogoImage}" Stretch="UniformToFill"/>
                </Border>
                <TextBlock Text="Ảnh Logo (Tùy chọn)" VerticalAlignment="Center" Margin="10,0,0,0" FontStyle="Italic"/>
            </StackPanel>

            <Button Content="TẠO MÃ QR" Command="{Binding GenerateQRCommand}"
                    Background="#007ACC" Foreground="White" FontWeight="Bold" Padding="15" Margin="0,20,0,0" FontSize="16"/>

        </StackPanel>

        <Border Grid.Column="1" BorderBrush="#FFC0CB" BorderThickness="2" Padding="20" Width="300" Height="380" VerticalAlignment="Top" CornerRadius="10" Background="WhiteSmoke">
            <StackPanel>
                <TextBlock Text="MÃ QR Thanh toán" FontWeight="Bold" HorizontalAlignment="Center" FontSize="16" Margin="0,0,0,15"/>
                <Image Source="{Binding QRCodeImage}" Width="260" Height="260" Stretch="Uniform"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>